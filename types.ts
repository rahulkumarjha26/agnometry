export interface NavLink {
  label: string;
  href: string;
}

export interface MetricCardProps {
  title: string;
  value: string;
  delay?: number;
}

export interface PhilosophyCardProps {
  text: string;
  delay?: number;
}
